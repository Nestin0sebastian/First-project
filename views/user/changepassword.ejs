<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Change Form</title>
    <style>


body {
    font-family: 'Arial', sans-serif;
    background: linear-gradient(to bottom, #ff9f43, #ffffff); /* Adjust colors as needed */
    color: #fff;
    margin: 0;
    padding: 0;

}



        body {
            font-family: 'Arial', sans-serif;
            background-color: #ffaa5a; /* Orange background */
            color: #fff;
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        .password-change-form {
            background-color: #fff; /* White background */
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
            overflow: hidden;
            width: 300px;
            padding: 20px;
            box-sizing: border-box;
        }

        .form-group {
            margin-bottom: 20px;
        }

        label {
            display: block;
            margin-bottom: 8px;
            color: #333; /* Dark text color */
        }

        input {
            width: 100%;
            padding: 10px;
            box-sizing: border-box;
            border: 1px solid #555;
            border-radius: 5px;
            background-color: #f1f1f1; /* Light gray background */
            color: #333; /* Dark text color */
        }

        .submit-button {
            background-color: #fc9700; /* Coral color */
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
        }
        body {
            font-family: 'Arial', sans-serif;
            background-color: #ffaa5a; /* Orange background */
            color: #fff;
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        .password-change-form {
            background-color: #fff; /* White background */
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
            overflow: hidden;
            width: 400px; /* Increase width to 400px */
            padding: 20px;
            box-sizing: border-box;
        }

        .form-group {
            margin-bottom: 20px;
        }

        label {
            display: block;
            margin-bottom: 8px;
            color: #333; /* Dark text color */
        }

        input {
            width: 100%;
            padding: 10px;
            box-sizing: border-box;
            border: 1px solid #555;
            border-radius: 5px;
            background-color: #f1f1f1; /* Light gray background */
            color: #333; /* Dark text color */
        }

        .submit-button {
            background-color: #fc9700; /* Coral color */
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
        }
        .back-to-home {
            background-color: #0c0fb1; /* Green background color */
            color: white; /* White text color */
            padding: 10px 20px; /* Padding around the text */
            font-size: 16px; /* Font size */
            border: none; /* No border */
            cursor: pointer; /* Cursor style on hover */
            text-decoration: none; /* Remove underline on the anchor */
            display: inline-block; /* Make the button inline block */
        }

        /* Add hover effect */
        .back-to-home:hover {
            background-color: #45a049; /* Darker green on hover */
        }
    </style>
</head>
<body>

    <div class="password-change-form">
        <h2>Password Change</h2>
        <form action="/changepasswordpost" method="post" onsubmit="return validateForm()">
            <div class="form-group">
                <label for="currentPassword">Current Password</label>
                <input type="password" id="currentPassword" name="currentPassword" required>
                <div class="error-message" id="currentPasswordError"></div>
            </div>
    
            <div class="form-group">
                <label for="newPassword">New Password</label>
                <input type="password" id="newPassword" name="newPassword" required>
                <div class="error-message" id="newPasswordError"></div>
            </div>
    
            <div class="form-group">
                <label for="confirmPassword">Confirm Password</label>
                <input type="password" id="confirmPassword" name="confirmPassword" required>
                <div class="error-message" id="confirmPasswordError"></div>
            </div>
            <a href="/profileget">
                <button type="button" class="back-to-home">Go Back </button>
            </a>
    
            <button type="submit" class="submit-button">Change Password</button>
        </form>
    </div>
    
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const form = document.querySelector('form');
            const errorMessage = document.createElement('div');
            errorMessage.className = 'error-message';
            errorMessage.style.color = '#ff6348'; /* Coral color */
    
            form.addEventListener('submit', function (event) {
                const currentPassword = document.getElementById('currentPassword').value;
                const newPassword = document.getElementById('newPassword').value;
                const confirmPassword = document.getElementById('confirmPassword').value;
    
                if (currentPassword.trim() === '' || newPassword.trim() === '' || confirmPassword.trim() === '') {
                    event.preventDefault(); // Prevent form submission
    
                    errorMessage.textContent = 'All fields must be filled.';
                    form.appendChild(errorMessage);
                }
            });
    
            // Add an event listener to each input to clear the error message when the user starts typing
            const inputFields = document.querySelectorAll('input[type="password"]');
            inputFields.forEach(function (input) {
                input.addEventListener('input', function () {
                    errorMessage.textContent = '';
                });
            });
        });









        function validateForm() {
    const currentPassword = document.getElementById('currentPassword').value.trim();
    const newPassword = document.getElementById('newPassword').value.trim();
    const confirmPassword = document.getElementById('confirmPassword').value.trim();

    // Clear previous error messages
    clearErrorMessages();

    let isValid = true;

    // Validate Current Password
    if (currentPassword === '') {
        displayErrorMessage('currentPassword', 'Current password is required.');
        isValid = false;
    }

    // Validate New Password
    if (newPassword === '') {
        displayErrorMessage('newPassword', 'New password is required.');
        isValid = false;
    }

    // Validate Confirm Password
    if (confirmPassword === '') {
        displayErrorMessage('confirmPassword', 'Confirm password is required.');
        isValid = false;
    }

    return isValid;
}

function displayErrorMessage(fieldId, message) {
    const errorElement = document.getElementById(`${fieldId}Error`);
    errorElement.textContent = message;
}

function clearErrorMessages() {
    const errorElements = document.querySelectorAll('.error-message');
    errorElements.forEach((element) => {
        element.textContent = '';
    });
}

    </script>
    
</body>
</html>
